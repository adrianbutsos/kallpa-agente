version: "3.8"
services:
  kallpa:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - ./knowledge:/app/knowledge
      - ./config:/app/config
      - ./templates:/app/templates
    restart: unless-stopped
